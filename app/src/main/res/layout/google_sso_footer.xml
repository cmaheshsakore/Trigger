<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sso_footer"
    android:layout_alignParentBottom="true" 
    android:background="#FFFFFF"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="48dp"
    android:orientation="vertical" >

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#CCC" />

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal" >
    
        
        <TextView
            android:id="@+id/signin_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/list_clickable"
            android:clickable="true"
            android:drawableLeft="@drawable/btn_g_normal"
            android:drawablePadding="8dp"
            android:gravity="left|center_vertical"
            android:minHeight="48dp"
            android:onClick="showGoogleSsoDialog"
            android:paddingLeft="16dp"
            android:text="@string/sign_in_with_google"
            android:textColor="#000000"
            android:textSize="18sp" />

        <ImageView
            android:id="@+id/sso_hide"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:background="@drawable/list_clickable"
            android:clickable="true"
            android:onClick="hideGoogleSsoFooter"
            android:padding="4dp"
            android:src="@drawable/ic_action_cancel" />
    </LinearLayout>

</LinearLayout>